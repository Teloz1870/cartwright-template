// Backward-compat re-export. PlanCard moved to components/shared/PlanCard.tsx
// so both admin chat and customer chat can reuse the same component.
export { default } from "@/components/shared/PlanCard";
