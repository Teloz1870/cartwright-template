/**
 * Route mount (cartwright-plugin-v1) — reviews plugin (customer submit).
 * Handler implementation: plugins/reviews/api/submit.ts.
 * Segment config stays literal here for Next's static analysis.
 */
export const runtime = "nodejs";

export { POST } from "@/plugins/reviews/api/submit";
