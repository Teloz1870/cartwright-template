/**
 * Route mount (cartwright-plugin-v1) — hoptify plugin (admin migration page).
 * Implementation: plugins/hoptify/admin/HoptifyAdminPage.tsx.
 * Segment config stays literal here for Next's static analysis.
 */
export const dynamic = "force-dynamic";

export { default } from "@/plugins/hoptify/admin/HoptifyAdminPage";
