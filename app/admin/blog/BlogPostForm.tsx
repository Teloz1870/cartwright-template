/**
 * Re-export shim — the form implementation moved to the blog plugin
 * (plugins/blog/admin/BlogPostForm.tsx, cartwright-plugin-v1). Keeps the
 * historical import path working unchanged for existing scaffolds.
 */
export { BlogPostForm } from "@/plugins/blog/admin/BlogPostForm";
