/**
 * Route mount (cartwright-plugin-v1) — google-workspace plugin (Sheets sync
 * admin page). Implementation: plugins/google-workspace/admin/sheets/SheetsAdminPage.tsx.
 * Segment config stays literal here for Next's static analysis.
 */
export const dynamic = "force-dynamic";

export { default } from "@/plugins/google-workspace/admin/sheets/SheetsAdminPage";
