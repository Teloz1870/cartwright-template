/**
 * Route mount (cartwright-plugin-v1) — thin page mount for the phone-widget
 * plugin. The dashboard implementation lives in the plugin's self-contained
 * module; this file only fixes the URL (`/admin/telefon`).
 */
export { default } from "@/plugins/phone-widget/admin/TelefonDashboard";
