/**
 * Re-export shim — the import-form client component moved to the
 * design-import plugin (plugins/design-import/admin/DesignImportForm.tsx,
 * cartwright-plugin-v1). Keeps the historical import path working unchanged
 * for existing scaffolds.
 */
export { DesignImportForm } from "@/plugins/design-import/admin/DesignImportForm";
